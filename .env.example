# =============================================================================
# gTAA AI Validator - Configuración LLM
# =============================================================================

# Proveedor de LLM (auto-detectado si no se especifica):
# - Si hay GEMINI_API_KEY: usa Gemini automáticamente
# - Si no hay API key: usa Mock (heurísticas deterministas)
# - Valores explícitos: gemini, mock
# LLM_PROVIDER=gemini

# =============================================================================
# Gemini (Cloud) - RECOMENDADO
# =============================================================================
# Obtener API key en: https://aistudio.google.com/api-keys
# Tier gratuito: ~15 requests/min, suficiente para demostración
#
# Si se agota el rate limit o hay error, el sistema usa MockLLMClient automáticamente.

# API key de Gemini (también acepta LLM_API_KEY como alias)
GEMINI_API_KEY=tu_api_key_aqui

# Modelo Gemini a usar (default: gemini-2.5-flash-lite)
# GEMINI_MODEL=gemini-2.5-flash-lite

# =============================================================================
# Mock (Heurísticas) - Sin dependencias externas
# =============================================================================
# Usa análisis determinista basado en AST + regex.
# Se activa automáticamente si no hay API key o si Gemini falla.
# Útil para: tests, CI/CD, demostración sin API key.
#
# Para forzar Mock explícitamente:
# LLM_PROVIDER=mock
